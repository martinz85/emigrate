/**
 * Layout for admin auth pages (login, reset-password)
 * This layout does NOT require authentication
 */
export default function AdminAuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}

